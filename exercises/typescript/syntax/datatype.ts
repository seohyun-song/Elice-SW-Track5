// boolean: true/false
let isDone: boolean = false;

// null: 값이 없다는 것을 명시
let n: null = null;

// undefined: 값이 할당하지 않은 변수의 초기값 * 값이 없다는 것을 명시하기 위해서는 null 사용
let u: undefined = undefined;

// number: 정수와 실수, Infinity, NaN
let decimal: number = 6;
let hex: number = 0xf00d;
let binary: number = 0b1010;

// string: 고유하고 수정 불가능한 데이터 타입이며 주로 객체 프로퍼티들의 식별자로 사용
let color: string = "blue";
let greeting: string = `Color is ${color}`;

// object: 객체형(참조형)
const obj: object = {};
